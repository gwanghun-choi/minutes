# 2026-08-24

## Goal

Three independent gaps, closed without letting them touch each other's
responsibility, API, or tests.

1. Uploading the same recording twice re-ran the whole analysis and left two
   meetings to tell apart by hand.
2. AI 후보정 repaired grammar around a mis-heard word instead of the word.
   The UAT case: `여보세요? 네, 전화를 받습니다.` / `네, 안녕하세요. 혹시 턱
   되실까요, 잠깐?` / `네.` came back as `혹시 턱 괜찮으실까요` — fluent, and
   nonsense for a call that has just connected.
3. Every folder in the sidebar had a count beside it; 전체 회의 and 미분류, the
   two rows above them, had nothing.

## Completed

### A. The same file, uploaded twice, is one meeting

Identity is `(owner_user_id, SHA-256 of the original bytes)` — see
[the decision record](../decisions/2026-08-24-duplicate-uploads-are-owner-scoped-by-content-hash.md)
for why it is owner-scoped and why the hash is not global.

- **Migration 012** adds `meetings.source_content_hash` (nullable, `CHECK` for
  64 lowercase hex) and a partial unique index on the pair,
  `WHERE source_content_hash IS NOT NULL`. Additive; nothing is backfilled, so
  every existing meeting keeps a NULL hash and takes part in no comparison.
- **The digest rides the write loop.** `create_meeting` already streamed the
  body a megabyte at a time; `hashlib.sha256().update(chunk)` goes in the same
  pass. No whole-file read, no second pass over the disk.
- **The check lands before the row.** Lookup → on a hit, `unlink` the file just
  written and answer `409`. No meeting, no version, and no `BackgroundTasks`
  entry: a guard that ran after `pipeline.process` was queued would have saved
  nothing, which is the whole point of the feature.
- **The index is the guarantee.** Two uploads that both pass the SELECT race to
  insert; the loser's `UniqueViolation` is caught and becomes the same 409
  rather than a 500.
- **409 body:** `detail` (worded by the existing meeting's status),
  `code: DUPLICATE_MEETING_SOURCE`, `existing_meeting_id`,
  `existing_meeting_title` (this account's own alias, via
  `organization.DISPLAY_TITLE`), `existing_meeting_status`. No hash, no path.
  `FAILED` is refused too and says to delete the meeting first; deletion is a
  hard `DELETE`, so it frees the bytes.
- **Frontend:** `ApiError` now carries the parsed error body, and the upload
  dialog turns a duplicate into the server's sentence plus a 기존 회의 보기
  button. Every other caller still reads `detail` alone. Upload stays one file
  at a time — no batch endpoint.

### B. 후보정 is context-aware post-correction, not proofreading

Root cause: the old prompt sent `"<번호>: <문장>"` with no speaker and no clock,
and its rules were "STT 오인식, 띄어쓰기, 명백한 용어 오류만" plus **"의미를
바꾸지 마세요"** — which is exactly the instruction that preserves `턱` and
repairs the grammar around it. Nothing said the token itself could be the error,
nothing asked whether the fix survived the reply, and nothing made abstaining an
outcome.

- **`assist._render_for_correction`** renders every utterance as
  `<번호> [mm:ss~mm:ss] <화자>: <문장>` in time order, reusing `rag._fmt_time`.
  Still one request for the whole meeting: every line therefore arrives with the
  line before and after it and the turn boundary between them, and a windowed
  call per segment would multiply an OpenAI request by the length of the
  recording for context this rendering already gives away.
- **`CORRECTION_PROMPT` rewritten** around three claims: the ASR token itself may
  be wrong (`기존 단어를 무조건 보존하지 마세요`); a fix must hold against the
  previous turn, the reply, and the topic, not just grammar; and abstaining is
  the default. Plus: no rewriting of 말투, no synonym swaps, no invented
  information, never flip negation or possibility, and a named conservative list
  (names, companies, products, numbers, amounts, dates, deadlines, places, terms,
  requests, decisions, assignees).
- **A correction must say why.** Each suggestion carries `reason`, and
  `suggest_corrections` drops any that arrives without one — the abstention rule
  enforced rather than trusted. Also dropped: an unknown sequence, a
  non-numeric sequence, unchanged text, and a second suggestion for a sequence
  already proposed.
- **HITL semantics unchanged.** Suggestions write nothing; 개별 반영 and 모두
  반영 edit the draft in the browser; only 수정 내용 저장 (the existing PATCH)
  persists; `_editable_draft` still gates the endpoint. 모두 반영 is safe
  because there is no uncertain tier in the response — an uncertain suggestion is
  simply not returned.
- **Frontend:** each card shows the reason under the before/after, and the empty
  state says why there is nothing rather than implying a failure.

### C. 전체 회의 and 미분류 carry counts

- **`organization.UNFILED`** is 미분류 as one predicate — the absence of a
  *categorised* filing, so an alias with no folder still counts as unfiled.
  `meetings._narrow` now pastes it instead of holding its own copy, which keeps
  the sidebar count and the `?category=none` page describing one set.
- **`organization.NAV_COUNTS`** is one aggregate over `access.READABLE`:
  `total` and `uncategorized`. `GET /api/meeting-categories` returns
  `{categories, total, uncategorized}` — two aggregate statements on one
  connection, neither growing with the tree.
- **Semantics left alone.** A folder's `meeting_count` is still *direct*
  membership, matching the rows the tree draws under it; the list page still
  means the subtree. Both counts are taken through `access.READABLE`, so a
  PENDING, REJECTED, or REVOKED share counts for nobody and another account's
  meeting is invisible in the number as well as on the screen.
- **One `Count` component** in `CategoryNav`, shared by all three kinds of row:
  same typography, same column, zero not drawn — the rule the tree already had
  for an empty folder, applied to the two fixed rows rather than a second rule
  beside it. Past 99 the label is `99+` (`lib/format.ts:countLabel`) and only
  then does the row carry a `title` with the real figure. Nothing truncates the
  count itself.
- **Invalidation.** Upload and delete now invalidate `keys.categories` as well
  as `keys.meetings`; filing, category CRUD, and accepting an invitation already
  did.
- Route state, `aria-current`, direct meeting placement, and `descendants=0` are
  untouched; the count is metadata inside the row's link and does nothing of its
  own.

## Verified

Ran here:

- **Backend collection + pure-logic tests:** `.venv/bin/python -m pytest tests -q`
  → **48 passed, 435 skipped**, 483 collected (was 451). Everything that needs
  PostgreSQL skipped: there is no database and no container runtime in this
  environment, so `conftest.db_available()` is False. The 32 new tests — 15 in
  `test_upload_dedup.py`, 7 in `test_assist.py`, 10 in `test_categories.py` —
  are collected and import-clean but **were not executed**.
- **Frontend:** `npm run typecheck`, `npm run lint`, `npm test`
  (**195 passed**, was 181), `npm run build` — all clean.

Not run, and why:

- **`python -m scripts.migrate`** — no reachable PostgreSQL. Migration 012 has
  not been applied or verified against a real database.
- **The 435 skipped backend tests** — same reason.
- **`npm run e2e`** — Playwright's Chromium is installed but cannot start:
  `libnspr4.so` is missing and `playwright install-deps` needs root. The new
  sidebar-count spec and the two renamed locators are unexecuted.
- **`docker compose config` / `build` / runtime smoke** — no Docker.
- **`python -m scripts.evaluate`** — retrieval, chunking, embedding, fusion, and
  the lexical analyzer are all untouched, so there is nothing to re-measure.
- **Human UAT for B** — the point of the change is that a text-only correction
  must not confidently propose an incoherent fix. Whether a real completion says
  `통화` or abstains is a judgement no mock can assert, and the tests deliberately
  pin the prompt and the validation instead.

## Blockers

- No PostgreSQL and no container runtime in this workspace: the backend suite,
  migration 012, and every duplicate/count assertion need a host that has one.
- Playwright cannot launch without system libraries that require root.

## Decisions

[2026-08-24-duplicate-uploads-are-owner-scoped-by-content-hash.md](../decisions/2026-08-24-duplicate-uploads-are-owner-scoped-by-content-hash.md).
B and C were not recorded: neither changes access, pipeline order, chunking,
embedding, retrieval, persistence, the execution model, or a dependency.

## Next

- Run the backend suite and migration 012 on a host with the database, and
  report the real passed/skipped counts.
- Raise `MIN_BACKEND_TESTS` in the `Jenkinsfile` from 451 to the confirmed count
  (483 collected here) — deliberately **not** changed in this wave.
- Run `npm run e2e` where Chromium can start.
- Human UAT: the three-turn phone-call sample for B, and the duplicate/count
  checklists for A and C.
- README carries the old `GET /api/meeting-categories` shape and says nothing
  about duplicate uploads or the navigation counts; it is a separate pass.

---

## Follow-up: the Jenkins run that this wave broke

Jenkins had a database, so it ran the 435 tests this workspace had skipped:
**479 passed, 4 failed, 0 skipped**. Both failures were expectations that had
gone stale against the new behaviour — no production code was involved, and none
was changed to fix them.

- `tests/test_migrate.py` — `VERSIONS` still ended at 011, so the three tests
  that assert the exact applied list, the exact recorded versions, and their
  count all failed on the extra `012_duplicate_upload_guard`. The list is now
  012-long, and `test_the_expected_list_is_every_migration_on_disk` asserts it
  against `MIGRATIONS/*.sql`, so the next migration fails one obvious test
  instead of three unrelated ones. `BEFORE_PERSONAL` derived itself as
  "everything not 011" and would have quietly picked 012 up as a *pre*-011
  migration; it is now `v < "011"`.
- `tests/test_categories.py::test_an_upload_without_a_date_stays_null` — its
  helper sent the same one byte for both uploads, so the second was correctly
  refused: `409 DUPLICATE_MEETING_SOURCE`, and `blank.json()["id"]` raised
  `KeyError`. That test is about `held_at`, not about identity, so the helper
  takes the payload as a parameter and the two uploads send `b"\x00"` and
  `b"\x01"`. The dedup suite keeps sending identical bytes, which is its whole
  subject.

## Verified (follow-up)

- `pytest tests -q` against a real PostgreSQL 18: **484 passed, 0 failed,
  0 skipped**, migration 012 applied by the runner. This is the first honest
  backend run of this wave — the earlier 48/435 entry above was a skip, not a
  pass.
- Frontend: `typecheck`, `lint`, `build` clean; Vitest **195 passed / 11 files**.
- `npm run e2e`: **21 passed**, including 사이드바 전체 회의와 미분류 개수가
  정리에 따라 움직인다. Chromium runs once the browser libraries are on
  `LD_LIBRARY_PATH`; it still cannot be installed system-wide without root.
- `git diff --check` clean.

`MIN_BACKEND_TESTS` is now **484** — measured passed, not collected — approved
on the strength of that run. The three comments that quoted the old 451 baseline
were corrected to the same measurement (48 of 484 run without a database, 436
skip, migrations 001..012). Nothing else in the pipeline moved: `pollSCM`, the
skip check, the floor check, Harbor, and the deploy repository are untouched.

---

## Follow-up: 삭제 becomes one control with two meanings

### Goal

The meeting detail page carried the most destructive action on the site as a red
button in a 관리 panel at the bottom of 개요 — past the summary a reader
scrolls through — while the row menu it shares with every list had grown a 삭제
of its own. And a shared reader had no way at all to get a meeting off their own
screen: the only exits were the owner's revoke and the owner's delete.

### Completed

- **One action cluster in the header.** `features/meetings/MeetingActions.tsx`
  (was `DangerZone.tsx`) draws `[검색 인덱스 다시 생성] [⋯]`. 삭제 is in the
  `⋯` and nowhere else; the 관리 panel is gone. Re-embedding is still owner-only
  and still approved-only, and is no longer drawn for a shared reader, who was
  only ever being offered a 403.
- **The reader's door.** `DELETE /api/meetings/{id}/shares/me` removes the
  caller's own ACCEPTED `meeting_shares` row, plus their `user_meeting_filing`
  and `meeting_user_speakers` rows, and touches nothing else. The owner gets 403
  and a stranger 404. No migration: an absent row already means "not shared",
  and REVOKED is the owner's word for the owner's act. See
  [the decision record](../decisions/2026-08-24-a-reader-can-hand-a-share-back.md).
- **One dialog for both.** `features/meetings/DeleteMeeting.tsx` is mounted by
  the list, the sidebar tree and the detail header, and `is_owner` picks the
  mutation and the wording — 원본 회의와 다른 사용자의 공유에는 영향을 주지
  않습니다 for a reader, the recording/index/insight/share sentence for the
  owner. Both invalidate `keys.meetings` and `keys.categories`, so 전체 회의 and
  미분류 move without a reload.

### Verified

- `pytest tests -q`: **497 passed, 0 failed, 0 skipped** (484 → +13 in
  `tests/test_sharing.py`).
- Frontend: `typecheck`, `lint`, `build` clean; Vitest **202 passed / 11 files**
  (195 → +7); `npm run e2e` **22 passed** (21 → +1).
- `git diff --check` clean.

### Next

- `MIN_BACKEND_TESTS` in the `Jenkinsfile` should go 484 → **497** once CI
  confirms the count; not changed here.
- README still describes the 관리 panel's 회의 삭제 button and knows nothing
  about `DELETE /shares/me`. Separate pass.
