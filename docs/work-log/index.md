# Work log

One file per day: `YYYY-MM-DD.md`. Newest first.

| date | summary |
|---|---|
| [2026-08-23 (wave 4)](2026-08-23.md#2026-08-23-wave-4--immutable-minutes-personal-filing-and-the-chat-surfaces) | Eleventh wave, turning what the human UAT settled into the source. Two things it found were the same shape of mistake: the product treated something one person decided as a property of the recording. **Approved minutes are now immutable** — `meetings._editable_draft` opens only for a `DRAFT` on a `REVIEW_REQUIRED` meeting and is the single gate in front of the transcript PATCH, the speaker rename, the correction suggestions, and the approval, so a direct API call is refused by the condition the screen was drawn from; the revision endpoints are gone (`405`) and the previous wave's `회의록 수정` workflow with them, while `meeting_versions` and the per-version transcript stay read-only because a database that ran that build may hold a v2 an old citation rests on. The stated cost: a transcript found wrong after approval is replaced by re-uploading the audio. **Filing became personal** (migration 011): `user_categories` is one tree per account, `user_meeting_filing` is `(user_id, meeting_id) → category_id, alias`, conversations file in the same tree, and every category reference is a composite foreign key carrying `user_id` so the database refuses a cross-account filing. `PUT /category` and the new `PUT /alias` take **read** access — a shared reader arranges their own screen and the owner's does not move — and organisation is never permission: after a revoke the reader's filing survives, the folder counts zero, and every door still answers 404. The backfill moves each owner's `meetings.category_id` into their own tree with ancestors, gives another account nothing, and skips an orphan; the global tables stay, unread. Three routes went with the model: category CRUD moved into the sidebar tree, an invitation became a notification instead of a page, and the version panel was deleted. The chat surfaces followed — an answer is prose rather than a full-width card, the 출처 drawer is always mounted and slides on `translate-x` as an overlay (toggle, `[N]` focus, ESC, `inert`+`aria-hidden` when closed), and its count is what the answer **cited** while the drawer still holds every retrieved candidate and says how many were not quoted. Retrieval SQL, constants, and prompts are untouched. Verified: 436 backend tests (1 pre-existing PostgreSQL 18 SQLSTATE artifact) including 13 new organisation tests, 13 rewritten immutability tests, and 8 migration tests over a schema built from 001–010 with data; 151 Vitest, 16 Playwright, typecheck, lint, production build. Not verified here: `docker compose build`, a runtime smoke, browser UAT, and `scripts.evaluate` — no Docker, no shared database, and no reason to re-measure retrieval that did not change. |
| [2026-08-23 (wave 3)](2026-08-23.md#2026-08-23-wave-3--the-compose-network-matches-the-deployment) | Tenth wave, one file: `compose.yaml` declared no networks, so Compose invented an implicit `minutes_default` per project. The running container reached PostgreSQL only because it had been attached to `minutes-net` by hand at some earlier point, outside the repository — so `docker compose run --rm minutes python -m scripts.migrate` created a fresh container on `minutes_default`, where `minutes-postgres` is not a name that resolves, and failed with `Temporary failure in name resolution` that read as a database outage. The same image under `docker run --network minutes-net` resolved it and reached 5432, which is what ruled out the database, the image, and the migration. `networks.default` now points at `minutes-net` as `external: true`: overriding the *default* rather than adding a per-service network is what puts one-off `run` containers on it too, and `external` keeps a shared database's network out of this project's lifecycle — at the price of one idempotent `docker network create` before the first `up`, now the first step of the README's deployment procedure. `.env.example`, which still described the retired `didim_api` instance with an empty host, was corrected to the container name, and `app/config.py`'s fallback for an unset `DATABASE_NAME` — `didim_api`, the last reference in code to a database that no longer exists — became `minutes`. No schema, migration, or image content changed, and no test pinned the old default. Verified here only as structure — one service, no service-level networks, no `minutes_default` — plus the unchanged backend and frontend gates; `docker compose config` and the deploy itself must run on the host, because there is no Docker in this environment. |
| [2026-08-23 (wave 2)](2026-08-23.md#2026-08-23-wave-2--ownership-sharing-versioning) | Ninth wave: meetings became owned assets, shareable by invitation, and revisable without leaving search. One access predicate — `app/services/access.py:READABLE` (owner, or an `ACCEPTED` row in `meeting_shares`) — is pasted by the meeting list, the detail page, the category counts, and all four retrieval paths, so "who may read this" cannot come to mean two things; a meeting you may not read is a `404` everywhere, and an orphan (a pre-009 row whose uploader migration 009 could not prove) is readable by nobody rather than by everybody. Sharing is an invitation to one `users.id`, never a link: `COMPLETED` meetings only, accept/reject by the invited account, revoke by the owner, and one row per pair with `UNIQUE` and a self-invitation `CHECK` in the database. Revocation is immediate at every door and reaches stored chat evidence, which is filtered on read so a citation keeps its `[N]` and loses its excerpt. Correcting approved minutes is now a new version rather than an edit: `meeting_versions` plus a `version` column on segments, chunks, and facts; embedding happens before the transaction that swaps the chunks and flips the published pointer together, so the previous version keeps answering through the draft, through the indexing, and through a failure — `meetings.status` deliberately stays `COMPLETED` because it is a retrieval predicate. Sharing is on the meeting, so a reader moves to the new version with no new invitation and never sees a draft. Being shared a meeting still says nothing about having been in it: `meeting_user_speakers` is untouched and `NO_IDENTITY` still refuses to guess. Migration 010 seeds `user2` the way 003 seeded `user`. Verified: 417 backend tests (1 pre-existing PostgreSQL 18 SQLSTATE artifact), 65 of them new, plus 12 new migration tests over a database built from 001–008 with data; a live two-user run as `user`/`user2`, 66/66; 150 Vitest, 16 Playwright, typecheck, lint, production build. Not verified here: `docker compose build`, a runtime smoke, and browser UAT — no Docker and no shared database in this environment. |
| [2026-08-23](2026-08-23.md) | Eighth wave, closing the real-meeting UAT list. The self-scope judgement moved out of the planner LLM into `rag.is_self_scoped`, a deterministic match on explicit first-person forms in the question — a general question is never again refused with `NO_IDENTITY`, and the same question cannot be judged two ways; the field was removed from `PLAN_PROMPT` entirely. Chat evidence became the 출처 panel beside the conversation (`SourceDrawer.tsx`), opened by `출처 N개` or by any `[N]` citation in the answer, with a link to the cited position in the transcript — retrieval, the `[근거]` prompt block, and the stored `sources` JSONB unchanged. Question and answer were given distinct shapes. `GET /api/meetings` became one page (`{items, total, page, page_size}`) narrowed by PostgreSQL through a single `_narrow` predicate shared by the COUNT and the page, with the list's whole query state in the URL; the filter bar's full-width rows turned out to be a CSS specificity bug in `controls.tsx` (`w-full` in the base class outranking every caller's width). Categories became a tree — migration 008 adds `parent_id` with `ON DELETE RESTRICT`, every existing category stays a root, one recursive CTE (`SUBTREE`) serves both the descendant filter and the cycle check, and a parent with children is refused rather than cascaded — navigable from a tree in the app's one sidebar. Meeting deletion lost its status gate so a meeting stranded at `DIARIZING` by a restart can be removed, with the pipeline cooperating (`set_status` reports a missing row, `_abandon` drops the audio) instead of the API refusing. Verified: 301 backend tests pass with one pre-existing environment failure (local pgvector 0.5.0 vs the pinned 0.3.6), 121 Vitest, 16 Playwright, typecheck, lint, and production build; migration applied fresh, re-run, and over existing flat categories. Not verified here: `docker compose build`, a runtime smoke, and browser UAT — no Docker, and no shared database, in this environment. |
| [2026-08-21](2026-08-21.md) | Database DDL removed from application startup and replaced by an explicit migration runner (`scripts/migrate.py` + `scripts/migrations/`) with `schema_migrations` version tracking; `minutes.users` became the source of truth for accounts (`display_name`, `is_active`, `updated_at`, `last_login_at`, migration-seeded POC account), replacing the `MINUTES_BOOTSTRAP_*` startup bootstrap. Second wave: the chat scope dialog fixed (`.modal[hidden]`) and Meeting Intelligence added — `meeting_facts` / `meeting_fact_participants` / `meeting_user_speakers` extracted from the approved transcript, with relationship, chronology, and follow-up-resolved retrieval under the existing scope rules. Third wave: an ACTION_ITEM recall failure found in NCP UAT traced to the extraction prompt (validation, dedupe, and windowing each ruled out empirically). Fourth wave: the whole UI migrated from Jinja2 + vanilla JS to a React + TypeScript SPA built by Vite, served by the same FastAPI process from the same image and origin, with the design redrawn as an operations tool. Fifth wave: the double sidebar collapsed into one app shell holding the chat list, the conversation given a single centre axis with a sticky centred composer, chat evidence reduced to two representative sources behind 근거 N개 더 보기 (retrieval, prompt, and stored provenance unchanged), the scope dialog rebuilt around explicit 전체/선택한 회의 modes with search, category, and range filters, and meeting organization added — `meeting_categories` as one nullable `ON DELETE SET NULL` FK, category CRUD, a compact meeting filter toolbar with sort and filter chips, and an optional `held_at` on upload that the browser pre-fills with today while nothing backfills existing NULL rows. Sixth wave: chat evidence became closed-by-default behind 근거 N개 보기 and opens whole with untruncated quotations (retrieval, prompt, and stored provenance again unchanged); question, answer, evidence, and the two backend-written notices were given four distinct shapes; conversations became renameable through a row menu and `PATCH /api/chat/sessions/{id}/title`, which the first-question auto-title cannot overwrite; the meeting toolbar was compacted with sort separated from the filters; category CRUD moved out to its own `/categories` route, leaving the list with a quiet link; and an unapproved meeting's overview and intelligence panels replaced skeleton rows with one shared notice naming the status, the reason it is empty, and the next human action. Seventh wave: retrieval measured before it was changed — an evaluation set (9 meetings, 44 questions, real BGE-M3 and real Kiwi in a throwaway schema) established a dense-only baseline, and a Korean lexical axis (Kiwi morphemes into a generated `tsvector` with a GIN index, migration 007) was fused with it by Reciprocal Rank Fusion plus a metadata-agreement boost, taking hit@3 from 0.927 to 1.000 and MRR from 0.896 to 0.911 while hit@1 fell 0.854 to 0.829; citation validation and server-side conflict detection added; chunk-level provenance (`chunks.source_segment_ids`) added. Generation-level metrics (no-answer, conflict) remain unmeasured — the development OpenAI key returns 401. Five approved meetings were lost from the shared `minutes` schema during the session; the cause was not identified, and the test suite and the evaluation harness were both experimentally exonerated. |
| [2026-08-20](2026-08-20.md) | MVP implemented and committed (`0331794`); repository governance and `docs/` established; HITL transcript review gate added; first NCP deployment; `pyannote.audio` 4.0.0 → 4.0.7 for the torch 2.13 checkpoint-load failure; real-audio E2E UAT on NCP (diarization → HITL gate → indexing → RAG); re-embedding added for approved meetings; meeting deletion added; Productization Wave 1 — POC login, persistent multi-turn chat, meeting scope picker with explicit-only global fallback, meeting summary, AI transcript correction, speaker colours. |

## How to use it

After meaningful work, append to today's file under the matching heading. Create
the file from the structure used by the entries above if it does not exist yet,
and add a row here.

Record:

- **Goal** — what the work was for
- **Completed** — what actually changed
- **Verified** — the command run and its real result, including SKIP and FAIL
- **Blockers** — what is stopping progress, and what would unblock it
- **Decisions** — a link to `../decisions/`, if one was recorded
- **Next** — what follows

Do not record: shell transcripts, per-file diffs, typo fixes, a copy of
`git log`, or every command executed. The work log is not a substitute for git
history — it captures why and what was verified, which git does not.

Only facts confirmed from the repository, its tests, or an observed run. If
something was not verified, write that it was not.
